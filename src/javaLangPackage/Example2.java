package javaLangPackage;

interface StringOperation{
    String getDescription();
}

// enums are classes as well.
// Inheritance is not allowed in classes
// When we compile an enum,
/** the Java compiler does some magic to it:
    1. It turns the enum into a subclass of the abstract class java.lang.Enum
    2. It compiles the enum as a final class
*/
public class Example2 {
    public static void main(String[] args) {

    }
}
